@{
    Layout = null;
}
<!doctype html>
<html lang="vi">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Quét QR</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container py-5 text-center">
        <h3>Quét QR trên bàn để gọi món</h3>
        <p class="text-muted">Mở camera trên điện thoại, quét mã → chạm vào liên kết.</p>
        <p class="small">Hoặc truy cập: <code>/c?code=T01</code> hoặc <code>/c/T01</code>.</p>
    </div>
</body>
</html>

